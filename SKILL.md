---
name: auto-music-download
description: 多源无损音乐下载；用户说"我想听/想听/来一首/下载/添加"某首歌时，下载 FLAC/无损音乐到 NAS Daoliyu/Navidrome 媒体库。
category: media
---

# auto-music-download - 无损音乐下载

触发：用户说"我想听XX""想听XX""来一首XX""帮我放XX""听一下XX"。
含义：下载到 NAS 音乐库，不是 Spotify 播放。禁止二次确认"是要播放还是下载"。

## 执行流程（唯一路径，禁止偏离）

```bash
python3 $HOME/.openclaw/skills/media/auto-music-download/scripts/music-fast-download.py "歌名" "歌手"
```

脚本内部已完成：多源搜索(netease优先,kuwo保底) → 防冒名过滤(指定歌手时只接受精确匹配) → NAS直接下载FLAC → ffprobe 校验真实 FLAC → mutagen 本地写元数据+封面(不依赖 MusicTag 容器) → 歌词补全（外置 .lrc + FLAC 内嵌）→ Navidrome扫库。

**绝对禁止做的事**（违反任何一条视为执行失败）：
- 禁止 SSH 到 NAS 做任何操作（脚本已处理）
- 禁止 web_search / search_files / read_file 查源码
- 禁止加载 references 文件（除非脚本报错且错误信息不明确）
- 禁止手写 Python / SQL / shell 命令
- 禁止调用 spotify 工具
- 禁止查询 Navidrome 数据库

## 歌手推断规则

用户没指定歌手时，如果歌名是知名歌曲（如"稻香""红尘客栈""晴天"），直接传歌手参数：
- 能从常识推断歌手 → 传 `"歌名" "歌手"`
- 不确定歌手 → 只传 `"歌名"`，接受脚本返回的第一个结果

## 回复格式

执行前一行：`搜索歌曲 → 下载 → 刮削 → 入库`
执行后只报告脚本输出结论，不做额外解释。

## 已知缺陷
脚本只重启 Navidrome 触发其重新扫库，但**不会触发 Daoliyu 重新扫库**。Daoliyu 使用自己的 ScanTask 系统，需要手动触发全量扫描后才可见新下载的文件（通常扫库间隔超过一个月时问题显著）。
触发方法：SSH 到 FN-NAS，调用 Daoliyu 后端 API 或直接通过容器执行全量扫描。

## 失败处理（最多重试 1 次）

- 脚本输出"疑似冒名资源/官方版本可能受版权限制" → **如实告知用户**：官方版本被
  版权(VIP)锁定，免费音源只剩蹭名假歌（同名不同曲，如"周杰伦-A-LNK"）。
  禁止擅自用 `MUSIC_ALLOW_FUZZY_ARTIST=1` 重跑；只有用户明确说"假的也要/
  随便来一版"才可加该环境变量重试。
- 脚本输出"❌ 未找到" → 换歌手名/别名重试一次，仍失败则报告用户
- 脚本输出"❌ 下载失败" → 重试一次，仍失败则报告
- 脚本输出"❌ 未下载到有效 FLAC" → 不要说已完成；报告“未拿到无损版本”，并说明最后一个失败原因
- 脚本输出"⚠️ 刮削异常" → 不重试，报告"已下载，刮削稍后自动补全"
- 用户明确追问“是不是无损/为什么异常/刮削是否成功”时，必须核实实际文件格式和脚本输出；不能只重跑下载命令后猜测。普通下载失败时仍不要扩展调查。

## 性能目标

15 秒内完成。整个 turn 不超过 3 次 API 调用（路由→执行→报告）。
